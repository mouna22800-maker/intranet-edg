# Dépendances backend (FastAPI) — Électricité de Guinée Intranet
# Installation : python -m pip install -r requirements.txt

fastapi==0.138.2
uvicorn==0.49.0
starlette==1.3.1
pydantic==2.13.4

# Authentification / sécurité
PyJWT==2.13.0
bcrypt==5.0.0
python-dotenv==1.2.2
nh3==0.3.6              # assainissement HTML (anti XSS stocké)

# Formulaires multipart (upload de fichiers) et base MySQL
python-multipart==0.0.32
PyMySQL==1.2.0
DBUtils==3.1.0          # pool de connexions MySQL (performance)
